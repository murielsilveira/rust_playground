#![allow(dead_code)]

mod sandbox;

fn main() {
    // sandbox::traits::run();
    // sandbox::decimals::run();
    // sandbox::defaults::run();
    // sandbox::excel::run();
    sandbox::dates::run();
    // sandbox::csv::run();
}
