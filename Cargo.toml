[package]
name = "rust-playground"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_decimal = "1.30.0"
rust_xlsxwriter = "0.54.0"

chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"
